# ==============================================
# IMENSIAH Frontend - Environment Configuration
# ==============================================

# SETUP INSTRUCTIONS:
# 1. Copy this file to .env.local
# 2. Fill in all required values marked with <REQUIRED>
# 3. NEVER commit .env.local to version control

# ----------------------------------------------
# SUPABASE (Authentication & Database)
# ----------------------------------------------
# Get from: Supabase Dashboard > Settings > API
NEXT_PUBLIC_SUPABASE_URL=<REQUIRED>  # Example: https://xyz.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=<REQUIRED>

# ----------------------------------------------
# BACKEND API
# ----------------------------------------------
# Local development (default)
NEXT_PUBLIC_API_URL=http://localhost:8080

# Production (uncomment and update when deploying)
# NEXT_PUBLIC_API_URL=https://api.yourdomain.com

# ----------------------------------------------
# APPLICATION URLS
# ----------------------------------------------
# Local development
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Production (uncomment and update when deploying)
# NEXT_PUBLIC_APP_URL=https://yourdomain.com

# ----------------------------------------------
# STRIPE (Payment)
# ----------------------------------------------
# Get from: Stripe Dashboard > Developers > API keys
# Use PUBLISHABLE key (pk_test_... or pk_live_...)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=<REQUIRED>

# ----------------------------------------------
# FEATURE FLAGS (Optional)
# ----------------------------------------------
# Enable/disable features without code changes
# NEXT_PUBLIC_ENABLE_PAYMENT=true
# NEXT_PUBLIC_ENABLE_AUTH_REQUIRED=false
# NEXT_PUBLIC_ENABLE_ANALYTICS=true

# ----------------------------------------------
# ANALYTICS (Optional)
# ----------------------------------------------
# Google Analytics
# NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# PostHog (optional)
# NEXT_PUBLIC_POSTHOG_KEY=<optional>
# NEXT_PUBLIC_POSTHOG_HOST=https://app.posthog.com

# ----------------------------------------------
# SENTRY ERROR TRACKING (Optional)
# ----------------------------------------------
# NEXT_PUBLIC_SENTRY_DSN=<optional>
# SENTRY_AUTH_TOKEN=<optional>

# ==============================================
# QUICKSTART FOR LOCAL DEVELOPMENT:
# ==============================================

# 1. Ensure backend is running:
#    - Backend should be at http://localhost:8080
#    - Check health: curl http://localhost:8080/health

# 2. Install dependencies:
#    $ cd frontend
#    $ npm install

# 3. Start development server:
#    $ npm run dev

# 4. Frontend runs on: http://localhost:3000

# 5. Test Integration:
#    - Visit: http://localhost:3000
#    - Submit form (test Stripe in test mode)
#    - Login at /auth/login
#    - Admin at /admin (requires admin role in Supabase)

# ==============================================
# NOTES:
# ==============================================
# - All NEXT_PUBLIC_* variables are exposed to browser
# - Keep secrets in backend only
# - Backend handles all sensitive operations
# - Frontend only needs public keys and URLs

# ----------------------------------------------
# FRONTEND TEST MODE (MOCK AUTH + DATA)
# ----------------------------------------------
# When true, the UI uses mocked auth and API responses so you can
# navigate all pages without real backend/auth.
# Do NOT enable in production builds.
NEXT_PUBLIC_TEST_MODE=false
# Default mock role: user | admin
NEXT_PUBLIC_TEST_ROLE=user
